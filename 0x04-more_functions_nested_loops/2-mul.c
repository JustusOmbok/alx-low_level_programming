#include "main.h"

/**
 * mul - multiplies two numbers
 *@a: first digit to multiply
 *@b: second digit to be multiplied
 * Return: value of the two multiplied values
 */

int mul(int a, int b)
{
return (a * b);
}
